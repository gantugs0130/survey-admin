import React from "react";
import { Layout } from "antd";
const { Content } = Layout;

const Home = () => {
    return (
        <Layout className="site-layout">
            <div>
                <p>sss</p>
            </div>
        </Layout>
    );
};
export default Home;
